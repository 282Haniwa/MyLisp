#include <stdio.h>
#include "structure.h"

Cell *subr_atom(Cell *);
Cell *subr_car(Cell *);
Cell *subr_cdr(Cell *);
Cell *subr_cons(Cell *);
Cell *subr_eq(Cell *);
Cell *subr_eval(Cell *);
Cell *subr_numberp(Cell *);
